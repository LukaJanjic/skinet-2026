# docker-compose.yml
services:
  sql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    platform: "linux/amd64"
    ports:
      - "1434:1433"
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "NewStrongPassword@1"
      MSSQL_AGENT_ENABLED: "True"